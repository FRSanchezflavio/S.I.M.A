import React from 'react';

// Componente simplificado que no requiere dependencias externas
const DatePickerProvider = ({ children }) => {
  return <>{children}</>;
};

export default DatePickerProvider;
